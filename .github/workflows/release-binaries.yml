name: Legacy Client Binary Release (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    name: Use the review-bound release finalizer
    runs-on: ubuntu-24.04
    steps:
      - name: Explain the supported release path
        shell: bash
        run: |
          echo "This legacy build-and-publish workflow is retired." >&2
          echo "Use 'Finalize Prebuilt Client Release' with a reviewed release contract." >&2
          exit 1
